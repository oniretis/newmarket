import type { StaffPermissions } from "@/types/staff";

export const ADMIN_STAFF_PERMISSIONS: StaffPermissions = {
  canDelete: true,
  canEdit: true,
  canView: true,
  canCreate: true,
};

export const VENDOR_STAFF_PERMISSIONS: StaffPermissions = {
  canDelete: true,
  canEdit: true,
  canView: true,
  canCreate: true,
};
