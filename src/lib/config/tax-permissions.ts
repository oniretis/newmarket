import type { TaxPermissions } from "@/types/taxes";

export const ADMIN_TAX_PERMISSIONS: TaxPermissions = {
  canDelete: true,
  canEdit: true,
  canView: true,
  canCreate: true,
};

export const VENDOR_TAX_PERMISSIONS: TaxPermissions = {
  canDelete: true,
  canEdit: true,
  canView: true,
  canCreate: true,
};
